{ 
   "version": "0.2.0",
    "configurations": [
        {
            "preLaunchTask": "build",
            "name": "C++ Launch",
            "type": "cppdbg",
            "request": "launch",
            "miDebuggerPath": "/usr/bin/gdb",
            "program": "${workspaceFolder}/build/NetTest",
            //"args": ["server"],
            "environment": [{ "name": "config", "value": "Debug" }],
            "cwd": "${workspaceFolder}"
        }
    ]
}
